services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    restart: unless-stopped
    user: "1000:1000"
    ports:
      - "8089:80"
    volumes:
      - /home/meshwa:/srv
      - ./data:/database
      - ./config:/config
